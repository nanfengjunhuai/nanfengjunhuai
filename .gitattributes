# Keep everything LF in the repo. On Windows, core.autocrlf rewrites text files
# to CRLF on checkout, which shows up as whole-file diffs every time the
# workflow commits a refreshed card — and the workflow itself is parsed on Linux.
* text=auto eol=lf

*.png binary
*.jpg binary
